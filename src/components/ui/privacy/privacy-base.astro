---
import Layout from '../../../layouts/PageLayout.astro';
import { SITE } from 'mars:config';

export interface Props {
  language?: string;
}

const { language = 'en' } = Astro.props;
const response = await fetch(`${SITE.site}/docs/privacy-policy-${language}.html`);
---

<Layout>
  <section class="privacy-policy">
    <div id="privacy-content" class="content" set:html={response} />
  </section>
</Layout>

<style>
  .privacy-policy {
    padding: 2rem;
  }
  .content {
    max-width: 800px;
    margin: 0 auto;
  }
</style>
